name: Run Automation Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Set up JDK
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      # 3️⃣ Install Chrome + ChromeDriver
      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install ChromeDriver
        run: sudo apt-get install -y chromium-chromedriver

      # 4️⃣ Install Firefox + GeckoDriver
      - name: Install Firefox
        run: sudo apt-get install -y firefox

      - name: Install GeckoDriver
        run: |
          wget https://github.com/mozilla/geckodriver/releases/download/v0.33.0/geckodriver-v0.33.0-linux64.tar.gz
          tar -xvzf geckodriver-v0.33.0-linux64.tar.gz
          sudo mv geckodriver /usr/local/bin/

      # 5️⃣ Install Edge + EdgeDriver
      - name: Install dependencies for Edge
        run: sudo apt-get update && sudo apt-get install -y libu2f-udev

      - name: Install Microsoft Edge
        run: |
          wget https://packages.microsoft.com/repos/edge/pool/main/m/microsoft-edge-stable/microsoft-edge-stable_139.0.3405.125-1_amd64.deb
          sudo dpkg -i microsoft-edge-stable_139.0.3405.125-1_amd64.deb
          sudo apt-get install -f -y

      - name: Install EdgeDriver
        run: |
          wget https://msedgedriver.azureedge.net/139.0.3405.125/edgedriver_linux64.zip
          unzip edgedriver_linux64.zip
          sudo mv msedgedriver /usr/local/bin/

      # 6️⃣ Run tests
      - name: Run tests with Maven
        run: mvn clean test

      # 7️⃣ Install Allure
      - name: Install Allure
        run: |
          wget https://github.com/allure-framework/allure2/releases/latest/download/allure-2.29.0.tgz
          tar -zxvf allure-2.29.0.tgz
          sudo mv allure-2.29.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      # 8️⃣ Generate Allure Report
      - name: Generate Allure Report
        run: mvn allure:report

      # 9️⃣ Upload Allure Report Artifact
      - name: Upload Allure Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: ./test-outputs/allure-report


